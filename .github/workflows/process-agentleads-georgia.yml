name: process-agentleads-georgia

on:
  workflow_dispatch:
    inputs:
      limit:
        description: 'Maximum number of records to process'
        required: false
        default: '3000'
        type: string
  schedule:
    # Run every hour
    - cron: "0 * * * *"

jobs:
  run-process:
    uses: ./.github/workflows/process-agentleads.yml
    with:
      state: "GA"
      limit: ${{ github.event.inputs.limit || '3000' }}
      batch_size: "100"
      verbose: true
    secrets: inherit
