name: public-tx-pc-postgres

on:
  workflow_dispatch:

jobs:
  run-process:
    uses: ./.github/workflows/process-csv-postgres-external.yml
    with:
      csv_path: "data/TX PC 2025.csv"
      state: tx
      speciality: pc
      email_column: "Email 1"
      npn_column: "Producer ID - NPN"
      lastname_column: "L Name"
    secrets: inherit
